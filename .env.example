GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite
GEMINI_TIMEOUT_MS=20000

# Firebase Console > 프로젝트 설정 > 클라우드 메시징 > 웹 구성 > 웹 푸시 인증서
FCM_VAPID_KEY=

# Firebase Console > 프로젝트 설정 > 서비스 계정 > 새 비공개 키 생성 (JSON 다운로드 후 경로 지정)
FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json
